#!/bin/sh
set -eu

repo_root=$(git rev-parse --show-toplevel)
cd "$repo_root"

echo "pre-commit: memperbarui snapshot database ter-sanitasi"
./scripts/export-database-snapshot.sh --stage
./scripts/verify-database-snapshot.sh
